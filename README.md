# Palindrom-check-app-
Palindrome Check App is a lightweight tool that checks whether a word, phrase, or number reads the same forward and backward. It ignores case, spaces, and punctuation, normalizes input, reverses it, and compares results. Ideal for learning string manipulation and basic algorithm design.
